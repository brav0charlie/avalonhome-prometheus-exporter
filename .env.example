# --- Miner Targets ---
# Set ONE of these and comment out the other (AVALON_IPS should be a comma
# separated list of IP addresses/hostnames):
AVALON_IP="192.168.1.50"
#AVALON_IPS="192.168.1.50,192.168.1.51,miner3.example.com"

# --- Miner API Port ---
AVALON_PORT=4028

# --- Miner API Timeout (optional) ---
# TCP connection timeout in seconds (default: 5.0)
#MINER_TIMEOUT=5.0

# --- Exporter Poll Interval (seconds) ---
UPDATE_INTERVAL=15

# --- Exporter Listen Port (inside container) ---
EXPORTER_PORT=9100

# --- Export Individual Chip Metrics ---
# WARNING: increases metric cardinality
# When true, exports per-chip metrics such as:
# - avalon_chip_temp_celsius
# - avalon_chip_voltage_volts
# - avalon_chip_work_nonce
EXPORT_CHIP_METRICS=false

# --- Logging Configuration (optional) ---
# Log level: DEBUG, INFO, WARNING, ERROR, CRITICAL (default: INFO)
#LOG_LEVEL=INFO
